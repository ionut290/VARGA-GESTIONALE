[package]
name = "varga-gestionale-desktop"
version = "0.1.0"
description = "Applicazione desktop Windows per Varga Gestionale"
authors = ["Varga Gestionale"]
edition = "2021"

[lib]
name = "varga_gestionale_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

